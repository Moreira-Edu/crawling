import * as dotenv from "dotenv";

dotenv.config();
export const {
  PAGE_URL,
  USER_ID,
  PASSWORD,
  USER_ID_INPUT,
  USER_PASSWORD_INPUT,
  LOGIN_BUTTON,
  USER_NAME,
  USER_EMAIL,
  USER_RA,
  USER_CYCLE,
  USER_PP,
  USER_PR,
  USER_MAX_PR,
  SEMESTER_COMPLETED,
  MAX_SEMESTER,
  LEAVE_SEMESTER,
  HISTORIC,
  SCHEDULE,
} = process.env;
